---
phase: 06-paper-and-supporting-information
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - paper/supporting_information.tex
autonomous: false
requirements: [PAP-02]

must_haves:
  truths:
    - "SI contains complete ODE derivation with all 14 state variables for single-eq model"
    - "SI contains 16-variable pre-equilibrium model for dithioester"
    - "ODE equations in SI match the implemented system in src/raft_ode.py line by line"
    - "SI contains dataset construction details: parameter ranges, LHS sampling, noise, failure rates"
    - "SI contains training hyperparameters table and bootstrap UQ protocol"
    - "SI contains all 12 per-class parity plots and 3 residual plots"
  artifacts:
    - path: "paper/supporting_information.tex"
      provides: "Complete Supporting Information LaTeX document"
      min_lines: 300
  key_links:
    - from: "paper/supporting_information.tex"
      to: "src/raft_ode.py"
      via: "ODE equations must match code implementation"
      pattern: "mu.*nu.*lam"
    - from: "paper/supporting_information.tex"
      to: "figures/parity_by_class/*.png"
      via: "includegraphics for 12 per-class parity plots"
      pattern: "parity_by_class"
---

<objective>
Write the complete Supporting Information document (LaTeX) containing the full ODE derivation, dataset details, training hyperparameters, bootstrap UQ protocol, and complete evaluation figures. The ODE equations MUST match src/raft_ode.py exactly (PAP-02).

Purpose: SI provides reproducibility-level detail that doesn't fit in the main manuscript.
Output: `paper/supporting_information.tex`
</objective>

<execution_context>
@C:/Users/xqk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xqk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-paper-and-supporting-information/06-CONTEXT.md
@.planning/phases/06-paper-and-supporting-information/06-RESEARCH.md
@src/raft_ode.py
@src/dataset_generator.py
@src/bootstrap.py
@checkpoints/bootstrap_summary.json
@checkpoints/calibration.json
@checkpoints/training_log.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Supporting Information LaTeX document</name>
  <files>paper/supporting_information.tex</files>
  <read_first>
    - src/raft_ode.py (FULL FILE — every ODE equation must be transcribed to LaTeX; 14-var single-eq function raft_ode_single_eq and 16-var pre-eq function raft_ode_preequilibrium)
    - src/dataset_generator.py (PARAM_BOUNDS dict: log10_Ctr=[-2,4], log10_kp=[2,4], log10_kt=[6,9], log10_kd=[-6,-4], I0=[0.001,0.05], f=[0.5,0.8], log10_cta_ratio=[-3,-1]; RAFT_TYPES list; TYPE_FIXED_PARAMS dict)
    - src/bootstrap.py (compute_jci function: F-dist with p=3, dfd=n-p; predict_with_uncertainty: 200 heads, backbone frozen)
    - checkpoints/bootstrap_summary.json (200 heads, 5 epochs/head, n_train=778963, n_val=97365, RTX 4090, 9h17m)
    - checkpoints/calibration.json (cal_factors=[100.0, 53.7, 3.5], coverage_before=[0.014, 0.071, 0.740], coverage_after=[0.692, 0.950, 0.950])
    - checkpoints/training_log.json (142 epochs, best at epoch 126)
    - .planning/phases/06-paper-and-supporting-information/06-CONTEXT.md (D-12: ODE derivation to reproducible level; D-13: full SI content list)
  </read_first>
  <action>
Write `paper/supporting_information.tex` as a standalone LaTeX document (achemso SI format).

**Section S1: RAFT Kinetic ODE Derivation** (per D-12, reproducible level):

Transcribe the COMPLETE moment equations from `src/raft_ode.py`. Must include:

S1.1 Single-Equilibrium Model (14 variables):
State vector: M, I, P, Int, mu0, mu1, mu2, nu0, nu1, nu2, lam0, lam1, lam2, CTA

For each variable, write the ODE in LaTeX matching the code:
- dM/dt = -kp * P * M
- dI/dt = -kd * I
- dP/dt = R_init + R_frag - R_add_cta - R_add_macro - 2*kt*P²
- dInt/dt = R_add_cta + R_add_macro - R_frag
- dCTA/dt = -kadd * P * CTA
- dmu0/dt = R_init + R_reinit_cta - 2*kt*mu0² - kadd*mu0*CTA + kadd*(mu0*nu0 - mu0*nu0)
- dmu1/dt = kp*M*mu0 - 2*kt*mu1*mu0 - kadd*mu1*CTA + kadd*(mu0*nu1 - mu1*nu0)
- dmu2/dt = kp*M*(2*mu1+mu0) - 2*kt*mu2*mu0 - kadd*mu2*CTA + kadd*(mu0*nu2 - mu2*nu0)
- dnu0/dt = kadd*mu0*CTA + 0 (exchange term cancels for k=0)
- dnu1/dt = kadd*mu1*CTA + kadd*(mu1*nu0 - mu0*nu1)
- dnu2/dt = kadd*mu2*CTA + kadd*(mu2*nu0 - mu0*nu2)
- dlam0/dt = 2*kt*mu0²
- dlam1/dt = 2*kt*mu1*mu0
- dlam2/dt = 2*kt*mu2*mu0

Explain the moment exchange model: d(mu_k)/dt_exchange = kadd*(mu0*nu_k - mu_k*nu0)

S1.2 Pre-Equilibrium Model (16 variables, dithioester only):
Additional variables: CTA_0 (initial RAFT agent), Int_pre (pre-equilibrium intermediate)
Additional rates: kadd0, kfrag0 (pre-equilibrium rate constants, kfrag0 << kfrag)
Explain how slow fragmentation of Int_pre causes inhibition period

S1.3 Observable Quantities:
- Conversion = 1 - M/M0
- Mn = (mu1 + nu1 + lam1) / (mu0 + nu0 + lam0) × M_monomer
- Đ = (mu2 + nu2 + lam2)(mu0 + nu0 + lam0) / (mu1 + nu1 + lam1)²
- Inhibition period: time to reach 1% conversion
- Retardation factor: R_p(with CTA) / R_p(without CTA) at 10% conversion

**Section S2: Dataset Construction**:
- Table S1: Parameter ranges (copy PARAM_BOUNDS exactly)
- LHS sampling strategy (scipy.stats.qmc.LatinHypercube)
- 4 RAFT types with fixed params per TYPE_FIXED_PARAMS
- Noise: sigma=0.03 (3% relative Gaussian)
- Total samples: ~973K (778K train / 97K val / 97K test)
- Storage: chunked HDF5, shape (n, 64, 64, 2), chunk=1000

**Section S3: Training Hyperparameters**:
- Table S2: optimizer=Adam, lr=3e-4, batch_size=256, epochs=142 (best at 126), early stopping patience, weighted MSE loss, stratified 80/10/10 split

**Section S4: Bootstrap UQ Protocol**:
- 200 bootstrap heads, backbone frozen, fc layer only
- 5 epochs per head, same optimizer settings
- F-distribution JCI: p=3, n=200, dfd=197, f_val=f.ppf(0.95, dfn=3, dfd=197)≈2.65
- half_width = sqrt(diag(Cov) * p * f_val / dfd)
- Post-hoc calibration: multiply half_width by cal_factors
- Table S3: calibration results (before/after coverage for each output)

**Section S5: Complete Evaluation Figures**:
- 12 per-class parity plots: figures/parity_by_class/{type}_{output}.png
- 3 residual plots: figures/residuals_{output}.png
- Include with \includegraphics, organized as Figure S1-S15

All equations must use consistent notation matching the code variable names.
  </action>
  <verify>
    <automated>test -f paper/supporting_information.tex && grep -c "\\\\frac\|\\\\dfrac\|dmu\|dnu\|dlam" paper/supporting_information.tex</automated>
  </verify>
  <acceptance_criteria>
    - paper/supporting_information.tex contains `\section` for ODE Derivation
    - paper/supporting_information.tex contains `mu_0` or `\mu_0` (moment notation)
    - paper/supporting_information.tex contains `nu_0` or `\nu_0` (dormant chain moments)
    - paper/supporting_information.tex contains `lam` or `\lambda` (dead chain moments)
    - paper/supporting_information.tex contains `k_{\mathrm{add}}` or `k_{add}` or `kadd` (RAFT addition rate)
    - paper/supporting_information.tex contains `k_{\mathrm{frag}}` or `kfrag` (fragmentation rate)
    - paper/supporting_information.tex contains `pre-equilibrium` or `Pre-Equilibrium` (dithioester model)
    - paper/supporting_information.tex contains `Latin Hypercube` or `LHS` (sampling method)
    - paper/supporting_information.tex contains `200` and `bootstrap` (UQ protocol)
    - paper/supporting_information.tex contains `parity_by_class` (per-class figure references)
    - paper/supporting_information.tex contains at least 10 `\frac` or `\dfrac` (ODE equations)
  </acceptance_criteria>
  <done>Complete SI with ODE derivation matching code, dataset details, training params, bootstrap protocol, and all evaluation figures</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ODE derivation verification against code</name>
  <what-built>Supporting Information with complete ODE derivation (14-var single-eq + 16-var pre-eq), dataset construction, training hyperparameters, bootstrap UQ, and evaluation figures</what-built>
  <how-to-verify>
    1. Compare SI Section S1 equations with src/raft_ode.py — every ODE must match
    2. Verify moment exchange term: d(mu_k)/dt = kadd*(mu0*nu_k - mu_k*nu0)
    3. Verify pre-equilibrium model adds CTA_0 and Int_pre with kadd0/kfrag0
    4. Check Table S1 parameter ranges match PARAM_BOUNDS in dataset_generator.py
    5. Check Table S3 calibration values match calibration.json exactly
    6. Verify all 15 SI figures are referenced (12 per-class + 3 residuals)
  </how-to-verify>
  <resume-signal>Type "approved" or describe equation mismatches</resume-signal>
</task>

</tasks>

<verification>
- paper/supporting_information.tex exists with all 5 sections
- ODE equations count matches code (14 ODEs for single-eq, 16 for pre-eq)
- Parameter ranges in Table S1 match PARAM_BOUNDS exactly
- Calibration values in Table S3 match calibration.json exactly
</verification>

<success_criteria>
Complete SI document with reproducible-level ODE derivation verified against code, all dataset/training/UQ details, and complete evaluation figures. User has verified ODE-code consistency.
</success_criteria>

<output>
After completion, create `.planning/phases/06-paper-and-supporting-information/06-03-SUMMARY.md`
</output>
