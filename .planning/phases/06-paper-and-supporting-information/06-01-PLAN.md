---
phase: 06-paper-and-supporting-information
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_figures.py
  - figures/fig1_concept.png
  - figures/fig2_ctfp_example.png
  - figures/fig3_parity_composite.png
  - figures/toc_graphic.png
autonomous: true
requirements: [PAP-01]

must_haves:
  truths:
    - "Figure 1 concept diagram exists showing experimental data → ctFP → SimpViT → 3 params + CI workflow"
    - "Figure 2 ctFP example exists showing dual-channel heatmap with Ch0=Mn and Ch1=Đ"
    - "Figure 3 composite parity plot exists combining log10_Ctr, inhibition_period, retardation_factor as (a)(b)(c) panels"
    - "TOC graphic exists at ~3.25×1.75 inch dimensions"
  artifacts:
    - path: "scripts/generate_figures.py"
      provides: "Figure generation script for all 4 new figures"
      min_lines: 150
    - path: "figures/fig1_concept.png"
      provides: "Concept/workflow diagram"
    - path: "figures/fig2_ctfp_example.png"
      provides: "Dual-channel ctFP heatmap example"
    - path: "figures/fig3_parity_composite.png"
      provides: "3-panel composite parity plot"
    - path: "figures/toc_graphic.png"
      provides: "TOC graphic for journal submission"
  key_links:
    - from: "scripts/generate_figures.py"
      to: "src/ctfp_encoder.py"
      via: "import encode_ctfp for Figure 2 generation"
      pattern: "from src.ctfp_encoder import"
    - from: "scripts/generate_figures.py"
      to: "figures/parity_*.png"
      via: "reads existing parity PNGs for composite Figure 3"
      pattern: "imread.*parity"
---

<objective>
Generate the four figures that don't yet exist: Figure 1 (concept/workflow diagram), Figure 2 (ctFP dual-channel example), Figure 3 (3-panel composite parity), and TOC graphic. All other figures (validation, residuals, per-class, loss curves) already exist.

Purpose: These figures are prerequisites for the manuscript — they appear in the main text and TOC.
Output: `scripts/generate_figures.py` + 4 PNG files in `figures/`
</objective>

<execution_context>
@C:/Users/xqk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xqk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-paper-and-supporting-information/06-CONTEXT.md
@.planning/phases/06-paper-and-supporting-information/06-RESEARCH.md

@src/ctfp_encoder.py
@src/raft_ode.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create figure generation script — concept diagram, ctFP example, composite parity, TOC</name>
  <files>scripts/generate_figures.py</files>
  <read_first>
    - src/ctfp_encoder.py (encode_ctfp function signature and channel definitions — Ch0=Mn/Mn_theory, Ch1=Đ clipped at 4.0)
    - src/raft_ode.py (simulate function signature — needed to generate a representative ODE curve for Fig 2 input)
    - figures/parity_log10_Ctr.png (exists — will be composited into Fig 3)
    - figures/parity_inhibition_period.png (exists — will be composited into Fig 3)
    - figures/parity_retardation_factor.png (exists — will be composited into Fig 3)
    - .planning/phases/06-paper-and-supporting-information/06-CONTEXT.md (D-07 figure list, D-09 TOC spec)
  </read_first>
  <action>
Create `scripts/generate_figures.py` with four generation functions, all using matplotlib. Run with `python scripts/generate_figures.py` to produce all 4 figures.

**Figure 1 — Concept/Workflow Diagram** (`figures/fig1_concept.png`):
- Schematic flowchart using matplotlib patches and arrows (no external diagramming tool)
- Left: "Experimental Data" box showing (conversion, Mn, Đ) table icon
- Center-left: "ctFP Encoding" box with small 2-channel grid icon, arrow from data
- Center: "SimpViT" box (show "2-layer Transformer, 4 heads, ~877K params" subtitle)
- Right: Three output boxes stacked: "log₁₀(Ctr)", "Inhibition Period", "Retardation Factor", each with "± 95% CI" annotation
- Arrow flow left-to-right. Use clean academic style (no gradients, muted blues/grays)
- Size: 8×3 inches, 300 DPI, white background
- Per D-07: this is Figure 1 in the paper

**Figure 2 — ctFP Example** (`figures/fig2_ctfp_example.png`):
- Generate a representative dithioester sample using `src/raft_ode.py` simulate function with params: Ctr=1000 (log10=3.0), [CTA]/[M]=0.01, raft_type='dithioester', using default kinetic params
- Encode the simulated (conversion, Mn, Đ) data into a 64×64×2 ctFP using `src/ctfp_encoder.py`
- Display as side-by-side heatmaps: (a) Channel 0 — Mn/Mn_theory, (b) Channel 1 — Đ
- Use `imshow` with appropriate colormaps (viridis for Ch0, plasma for Ch1), colorbars, axis labels "x: [CTA]/[M] (normalized)" and "y: Conversion (normalized)"
- Title: "Chain Transfer Fingerprint (ctFP) — Dithioester, Ctr = 1000"
- Size: 8×4 inches, 300 DPI
- Per D-07: this is Figure 2 in the paper

**Figure 3 — Composite Parity** (`figures/fig3_parity_composite.png`):
- Load existing PNGs: `figures/parity_log10_Ctr.png`, `figures/parity_inhibition_period.png`, `figures/parity_retardation_factor.png`
- Arrange as 1×3 subplot grid using `plt.imread` + `ax.imshow`
- Add panel labels: (a), (b), (c) in top-left of each panel
- Size: 15×5 inches, 300 DPI
- Per D-07: this is Figure 3 in the paper

**Figure TOC** (`figures/toc_graphic.png`):
- Simplified version of Figure 1 concept diagram
- Compact: show ctFP grid icon → SimpViT box → "Ctr, t_inh, R_ret" output with CI bars
- Size: 3.25×1.75 inches (per D-09, ACS TOC spec), 300 DPI
- Minimal text, clean lines, readable at small size

All figures saved with `bbox_inches='tight'`, `dpi=300`, `facecolor='white'`.
  </action>
  <verify>
    <automated>cd C:/CodingCraft/DL/ViT-Ctr && python scripts/generate_figures.py && python -c "import os; files=['figures/fig1_concept.png','figures/fig2_ctfp_example.png','figures/fig3_parity_composite.png','figures/toc_graphic.png']; missing=[f for f in files if not os.path.exists(f)]; print('PASS' if not missing else f'MISSING: {missing}')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/generate_figures.py contains `def generate_fig1_concept(`
    - scripts/generate_figures.py contains `def generate_fig2_ctfp(`
    - scripts/generate_figures.py contains `def generate_fig3_composite(`
    - scripts/generate_figures.py contains `def generate_toc(`
    - figures/fig1_concept.png exists and is > 10KB
    - figures/fig2_ctfp_example.png exists and is > 10KB
    - figures/fig3_parity_composite.png exists and is > 10KB
    - figures/toc_graphic.png exists and is > 5KB
    - scripts/generate_figures.py contains `from src.ctfp_encoder import` or `from src import`
  </acceptance_criteria>
  <done>All 4 new figures generated as PNG files at 300 DPI, ready for inclusion in manuscript</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual review of generated figures</name>
  <what-built>Four publication figures: Fig 1 (concept workflow), Fig 2 (ctFP heatmap example), Fig 3 (3-panel composite parity), TOC graphic</what-built>
  <how-to-verify>
    1. Open `figures/fig1_concept.png` — verify workflow diagram is clear: data → ctFP → SimpViT → 3 outputs + CI
    2. Open `figures/fig2_ctfp_example.png` — verify dual-channel heatmaps show meaningful ctFP encoding (not blank/uniform)
    3. Open `figures/fig3_parity_composite.png` — verify 3 parity plots are arranged as (a)(b)(c) panels with labels
    4. Open `figures/toc_graphic.png` — verify it's ~3.25×1.75 in, readable at small size, captures the key concept
    5. Check all figures have white backgrounds and are print-quality (300 DPI)
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues with any figure</resume-signal>
</task>

</tasks>

<verification>
- All 4 PNG files exist in `figures/` directory
- `python scripts/generate_figures.py` runs without error
- Figure sizes match specifications (check with `python -c "from PIL import Image; img=Image.open('figures/toc_graphic.png'); print(img.size, img.info.get('dpi'))"`)
</verification>

<success_criteria>
Four publication-ready figures generated: concept diagram, ctFP example, composite parity, and TOC graphic. User has visually approved all figures.
</success_criteria>

<output>
After completion, create `.planning/phases/06-paper-and-supporting-information/06-01-SUMMARY.md`
</output>
