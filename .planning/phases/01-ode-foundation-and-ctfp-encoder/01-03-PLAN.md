---
phase: 01-ode-foundation-and-ctfp-encoder
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - src/raft_ode.py
  - src/diagnostic.py
  - tests/test_raft_ode.py
  - notebooks/01_ode_validation.ipynb
autonomous: false
requirements: [SIM-01, SIM-02, SIM-03, ENC-01]
must_haves:
  truths:
    - "ODE curves for 3 RAFT agent types qualitatively match published literature trends"
    - "Extreme-parameter limit checks pass (high Ctr -> low D, low Ctr -> high D)"
    - "1000-sample diagnostic dataset generates with < 2% failure rate across all 4 RAFT types"
    - "ctFP images from diagnostic dataset show physically reasonable dual-channel patterns"
  artifacts:
    - path: "src/diagnostic.py"
      provides: "Diagnostic dataset generation script"
      exports: ["generate_diagnostic_dataset"]
    - path: "notebooks/01_ode_validation.ipynb"
      provides: "Interactive validation plots comparing ODE output to literature trends"
    - path: "tests/test_raft_ode.py"
      provides: "Extended with diagnostic dataset smoke test"
      contains: "test_diagnostic_dataset"
  key_links:
    - from: "src/diagnostic.py"
      to: "src/raft_ode.py"
      via: "import simulate_raft, compute_retardation_factor, compute_inhibition_period"
      pattern: "from src.raft_ode import"
    - from: "src/diagnostic.py"
      to: "src/ctfp_encoder.py"
      via: "import transform for ctFP generation"
      pattern: "from src.ctfp_encoder import"
    - from: "notebooks/01_ode_validation.ipynb"
      to: "src/raft_ode.py"
      via: "import simulate_raft for validation plots"
      pattern: "from src.raft_ode import"
---

<objective>
Validate the ODE system against literature curves and extreme-parameter limits, then generate a 1000-sample diagnostic dataset to confirm numerical stability across the full parameter space before committing to million-sample generation in Phase 2.

Purpose: This is the final quality gate before Phase 2. Any ODE bugs caught here save a full dataset regeneration.
Output: Validation notebook with literature comparisons, diagnostic dataset script, extended tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-ode-foundation-and-ctfp-encoder/01-CONTEXT.md
@.planning/phases/01-ode-foundation-and-ctfp-encoder/01-RESEARCH.md
@.planning/phases/01-ode-foundation-and-ctfp-encoder/01-01-SUMMARY.md
@.planning/phases/01-ode-foundation-and-ctfp-encoder/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: src/raft_ode.py -->
Expected exports after Plan 01:
```python
def simulate_raft(params: dict, raft_type: str = 'ttc', t_end: float = 36000, n_conv_points: int = 50) -> dict | None:
    """Returns {'conversion': np.ndarray, 'mn': np.ndarray, 'dispersity': np.ndarray, 'mn_norm': np.ndarray} or None on failure"""

def compute_retardation_factor(params: dict, raft_type: str, conv_target: float = 0.5) -> float:
    """Returns Rp(RAFT)/Rp(no CTA) in (0, 1]"""

def compute_inhibition_period(sol_dense, M0: float, t_end: float) -> float:
    """Returns t_inh/t_end in [0, 1]"""
```

<!-- From Plan 02: src/ctfp_encoder.py -->
```python
def transform(data, img_size=64) -> torch.Tensor:
    """data: iterable of (cta_ratio_norm, conversion, mn_norm, dispersity). Returns (2, 64, 64) float32 tensor."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnostic dataset generator and ODE validation notebook</name>
  <files>src/diagnostic.py, notebooks/01_ode_validation.ipynb, tests/test_raft_ode.py</files>
  <read_first>
    - src/raft_ode.py (current implementation from Plan 01)
    - src/ctfp_encoder.py (encoder from Plan 02)
    - tests/test_raft_ode.py (existing tests to extend)
    - .planning/phases/01-ode-foundation-and-ctfp-encoder/01-RESEARCH.md (Literature Validation Targets section: System 1 CDB/Styrene, System 2 TTC/MMA, System 3 Xanthate/VAc; Diagnostic Dataset Design section; Rate Constant Ranges table)
    - .planning/phases/01-ode-foundation-and-ctfp-encoder/01-CONTEXT.md (D-07, D-08, D-10, D-11)
  </read_first>
  <action>
1. Create `src/diagnostic.py`:

   **Function `generate_diagnostic_dataset(n_per_type=250, seed=42)`:**
   - Per D-10: Generate 250 samples per RAFT type (dithioester, ttc, xanthate, dithiocarbamate) = 1000 total
   - Per Research "Diagnostic Dataset Design": 10 Ctr values x 25 [CTA]/[M] values per type
   - Ctr grid: np.logspace(-2, 4, 10) per D-07
   - CTA/M grid: np.logspace(-3, -1, 25) per D-08
   - Fixed kinetic parameters per type (from Research Rate Constant Ranges table):
     - TTC/MMA: kp=650, kt=1e8, kd=1.5e-5, f=0.5, ki=1e4, M_monomer=100.12
     - Dithioester/Styrene: kp=340, kt=1e8, kd=1.5e-5, f=0.5, ki=1e4, M_monomer=104.15, kadd0=1e6, kfrag0=1.0
     - Xanthate/VAc: kp=6700, kt=1e8, kd=1.5e-5, f=0.5, ki=1e4, M_monomer=86.09
     - Dithiocarbamate/VAc: kp=6700, kt=1e8, kd=1.5e-5, f=0.5, ki=1e4, M_monomer=86.09 (same as xanthate, lower kadd)
   - For each (Ctr, CTA_M_ratio): derive kadd from Ctr*kp (simplified), set kfrag=1e4
   - Call simulate_raft + compute_retardation_factor + compute_inhibition_period for each sample
   - Use joblib.Parallel(n_jobs=-1) with tqdm progress bar
   - Return dict with: params (list of dicts), results (list of dicts with conversion/mn/dispersity/mn_norm), labels (list of dicts with log10_ctr, inhibition_period, retardation_factor), ctfp_tensors (list of torch.Tensor from transform()), failures (list of failed param indices)
   - Print summary: total samples, success count, failure count, failure rate

   **Function `save_diagnostic(dataset, path='data/diagnostic_1000.npz')`:**
   - Save ctFP tensors as stacked numpy array, labels as structured array
   - Create data/ directory if needed

2. Create `notebooks/01_ode_validation.ipynb` (as a Python script that can be converted to notebook):

   The notebook should contain cells for:
   - **Cell 1:** Import src.raft_ode, matplotlib
   - **Cell 2:** Literature System 1 -- CDB/Styrene (dithioester) validation:
     - Simulate with params matching CDB/styrene at 60C: kp=340, kt=1e8, Ctr~20 (typical CDB), [CTA]/[M]=0.005
     - Plot Mn vs conversion and D vs conversion
     - Annotate expected behavior: "visible inhibition period", "D dip then rise", "Mn linear after inhibition"
   - **Cell 3:** Literature System 2 -- TTC/MMA validation:
     - Simulate with params matching dodecyl-TTC/MMA at 60C: kp=650, Ctr~50, [CTA]/[M]=0.01
     - Plot Mn vs conversion and D vs conversion
     - Annotate: "no inhibition", "D < 1.4", "linear Mn growth"
   - **Cell 4:** Literature System 3 -- Xanthate/VAc validation:
     - Simulate with params matching O-ethyl xanthate/VAc at 60C: kp=6700, Ctr~1, [CTA]/[M]=0.01
     - Plot Mn vs conversion and D vs conversion
     - Annotate: "D ~ 1.2 up to 65% then rises", "pseudo-first-order kinetics"
   - **Cell 5:** Extreme limit checks:
     - High Ctr (10000): D should be < 1.1 at moderate conversion
     - Low Ctr (0.01): D should be > 1.5, Mn should deviate from ideal
   - **Cell 6:** ctFP visualization for one sample from each system using matplotlib imshow

3. Extend `tests/test_raft_ode.py` with:
   - `test_diagnostic_dataset` marked `@pytest.mark.slow`: Run generate_diagnostic_dataset(n_per_type=250), assert failure_rate < 0.02 (< 20 failures out of 1000)
   - `test_diagnostic_labels_valid`: For successful samples, assert log10_ctr in [-2, 4], inhibition_period in [0, 1], retardation_factor in (0, 1]
  </action>
  <verify>
    <automated>cd C:/CodingCraft/DL/ViT-Ctr && pytest tests/test_raft_ode.py -x -v -k "not slow" && python -c "from src.diagnostic import generate_diagnostic_dataset; ds = generate_diagnostic_dataset(n_per_type=5); print(f'Success: {len(ds[\"results\"])} samples, {len(ds[\"failures\"])} failures')"</automated>
  </verify>
  <acceptance_criteria>
    - src/diagnostic.py contains `def generate_diagnostic_dataset(n_per_type=250`
    - src/diagnostic.py contains `from src.raft_ode import simulate_raft`
    - src/diagnostic.py contains `from src.ctfp_encoder import transform`
    - src/diagnostic.py contains `joblib.Parallel`
    - notebooks/01_ode_validation.ipynb exists (or notebooks/01_ode_validation.py)
    - tests/test_raft_ode.py contains `def test_diagnostic_dataset`
    - tests/test_raft_ode.py contains `pytest.mark.slow`
    - `python -c "from src.diagnostic import generate_diagnostic_dataset"` exits 0
    - Quick smoke test (n_per_type=5) completes without crash
  </acceptance_criteria>
  <done>
    Diagnostic dataset generator produces samples across all 4 RAFT types. ODE validation notebook shows qualitatively correct curves for 3 literature systems. Extended test suite includes diagnostic smoke test.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify ODE validation plots and diagnostic dataset quality</name>
  <files>notebooks/01_ode_validation.ipynb</files>
  <action>
    User visually verifies that ODE output curves match expected literature behavior for all 3 RAFT systems and that the diagnostic dataset passes the numerical stability threshold.
  </action>
  <verify>
    <automated>cd C:/CodingCraft/DL/ViT-Ctr && pytest tests/ -v</automated>
  </verify>
  <done>User confirms ODE curves match literature trends and diagnostic dataset has less than 2% failure rate.</done>
  <what-built>
    Complete RAFT ODE system with validation against 3 literature systems (CDB/Styrene dithioester, TTC/MMA trithiocarbonate, Xanthate/VAc). Diagnostic dataset generator covering full parameter space. ctFP encoder producing dual-channel fingerprints.
  </what-built>
  <how-to-verify>
    1. Open `notebooks/01_ode_validation.ipynb` (or run the .py equivalent)
    2. Check CDB/Styrene (dithioester) plot: Mn vs conversion should show delayed onset (inhibition), D should dip then rise
    3. Check TTC/MMA plot: Mn should be linear with conversion from the start, D should stay below 1.4
    4. Check Xanthate/VAc plot: D should be ~ 1.2 up to 65% conversion then gradually increase
    5. Check extreme limit plots: high Ctr gives very low D, low Ctr gives high D
    6. Check ctFP visualization: dual-channel heatmaps should show sparse but physically reasonable patterns
    7. Run `cd C:/CodingCraft/DL/ViT-Ctr && pytest tests/test_raft_ode.py tests/test_ctfp_encoder.py -v` -- all tests should pass
    8. Run `cd C:/CodingCraft/DL/ViT-Ctr && pytest tests/test_raft_ode.py -v -k slow` -- diagnostic dataset should complete with < 2% failure rate
  </how-to-verify>
  <resume-signal>Type "approved" if ODE curves match expected literature behavior and all tests pass. Describe specific issues if not.</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/ -v` -- all tests pass (ODE + encoder)
- `pytest tests/test_raft_ode.py -v -k slow` -- diagnostic dataset < 2% failure
- Notebook shows qualitatively correct ODE curves for 3 RAFT systems
- ctFP visualization shows reasonable dual-channel patterns
</verification>

<success_criteria>
- ODE reproduces expected Mn/D trends for dithioester (inhibition+retardation), TTC (clean control), xanthate (moderate control)
- 1000-sample diagnostic dataset generates with < 2% failure rate
- All tests pass (both ODE and encoder suites)
- User approves validation checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-ode-foundation-and-ctfp-encoder/01-03-SUMMARY.md`
</output>
