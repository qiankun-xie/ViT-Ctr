---
phase: 05-streamlit-web-application
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app.py
autonomous: false
requirements: [APP-01, APP-02, APP-03, APP-04, APP-05, APP-06]
must_haves:
  truths:
    - "User can manually enter experimental data rows in an editable table and click Predict"
    - "User can upload Excel/CSV and download a pre-formatted template"
    - "Invalid input shows red error banner listing all validation failures"
    - "Three prediction cards display Ctr (prominent), inhibition period, and retardation factor with 95% CI"
    - "Dual-channel ctFP heatmap renders below results with viridis and plasma colormaps"
    - "Model loads once via st.cache_resource, not on every rerun"
  artifacts:
    - path: "app.py"
      provides: "Complete Streamlit web application"
      min_lines: 150
      contains: "st.set_page_config"
  key_links:
    - from: "app.py"
      to: "src/app_utils.py"
      via: "imports validate_input, prepare_ctfp_input, generate_template, format_results"
      pattern: "from src.app_utils import"
    - from: "app.py"
      to: "src/ctfp_encoder.py"
      via: "imports transform for ctFP encoding"
      pattern: "from src.ctfp_encoder import transform"
    - from: "app.py"
      to: "src/bootstrap.py"
      via: "imports predict_with_uncertainty for inference"
      pattern: "from src.bootstrap import predict_with_uncertainty"
    - from: "app.py"
      to: "src/model.py"
      via: "imports SimpViT for model instantiation"
      pattern: "from src.model import SimpViT"
---

<objective>
Build the complete Streamlit web application wiring Plan 01 utilities to the model inference pipeline per 05-UI-SPEC.md.

Purpose: User-facing deliverable of Phase 5.
Output: app.py at project root, runnable via streamlit run app.py.
</objective>

<execution_context>
@C:/Users/Lucas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lucas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-streamlit-web-application/05-CONTEXT.md
@.planning/phases/05-streamlit-web-application/05-RESEARCH.md
@.planning/phases/05-streamlit-web-application/05-UI-SPEC.md
@.planning/phases/05-streamlit-web-application/05-01-SUMMARY.md

<interfaces>
From Plan 01 output src/app_utils.py:
  def validate_input(df) returns tuple of (pd.DataFrame or None, list of str)
  def prepare_ctfp_input(df, m_monomer) returns list of tuples for transform()
  def generate_template() returns BytesIO with xlsx content
  def format_results(mean_pred, half_width) returns dict with keys: ctr, log10_ctr, ctr_lower, ctr_upper, inhibition_period, inh_lower, inh_upper, retardation_factor, ret_lower, ret_upper

From src/ctfp_encoder.py:
  def transform(data, img_size=64) returns torch.Tensor shape (2, 64, 64)

From src/model.py:
  class SimpViT(img_size=64, patch_size=16, num_outputs=3, hidden_size=64, num_layers=2, num_heads=4)

From src/bootstrap.py:
  def predict_with_uncertainty(model, fp_tensor, bootstrap_ckpt, cal_factors, device) returns (mean ndarray(3,), half_width ndarray(3,))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete Streamlit app (app.py)</name>
  <files>app.py</files>
  <read_first>
    - src/app_utils.py (Plan 01 output)
    - src/ctfp_encoder.py (transform signature)
    - src/model.py (SimpViT constructor)
    - src/bootstrap.py (predict_with_uncertainty, lines 119-154)
    - .planning/phases/05-streamlit-web-application/05-UI-SPEC.md (layout flow, component inventory, result card CSS, heatmap spec, copywriting contract, data editor column config, interaction states)
    - .planning/phases/05-streamlit-web-application/05-CONTEXT.md (D-01 through D-14)
    - .planning/phases/05-streamlit-web-application/05-RESEARCH.md (Pattern 2: model loading, Pattern 3: data_editor, anti-patterns)
    - C:/CodingCraft/DL/ViT-RR/deploy.py (reference Streamlit implementation)
  </read_first>
  <action>
    Create app.py at project root following 05-UI-SPEC.md layout flow exactly. The app is a thin UI layer; all logic lives in src/ modules.

    PAGE CONFIG (D-11): st.set_page_config(page_title="ViT-Ctr: RAFT Chain Transfer Constant Predictor", layout="wide")

    MODEL LOADING with @st.cache_resource (D-14, APP-06): Decorator on load_model() function. Load SimpViT(num_outputs=3) with torch.load best_model.pth (weights_only=True), bootstrap_heads.pth (weights_only=False per Phase 3 decision), and calibration.json. Return (model, bootstrap_ckpt, cal_factors).

    CHECKPOINT FILE DETECTION: If best_model.pth missing: st.error with UI-SPEC missing checkpoints message, hide Predict. If bootstrap files missing but model exists: st.warning with UI-SPEC no bootstrap message, set has_bootstrap=False.

    TITLE (D-12): st.title("ViT-Ctr: RAFT Chain Transfer Constant Predictor"), st.caption("Simultaneously predict Ctr, inhibition period, and retardation factor from experimental kinetic data.")

    MONOMER SELECTOR (RESEARCH Open Q #1): st.selectbox with "MMA (100.12 g/mol)", "Styrene (104.15 g/mol)", "Vinyl Acetate (86.09 g/mol)", "Custom". Map to float. If Custom: st.number_input min_value=1.0, value=100.0, step=0.01.

    INPUT TABS (D-03): st.tabs(["Manual Input", "File Upload"])

    TAB 1 MANUAL INPUT (D-01, APP-01): st.data_editor with num_rows="dynamic", hide_index=True, key="data_input". Default empty DataFrame with columns [CTA]/[M], conversion, Mn, D (float64). column_config per UI-SPEC: NumberColumn for each with help text, min/max values, format strings.

    TAB 2 FILE UPLOAD (D-02, D-04, APP-02): st.download_button for Excel template via generate_template(). st.file_uploader for xlsx/csv. On upload: read with pandas, store in session_state, display in data_editor for review.

    PREDICT BUTTON (D-05): st.button("Predict", type="primary", use_container_width=True).

    ON PREDICT: 1) validate_input(df), show st.error if errors. 2) prepare_ctfp_input(df_valid, m_monomer). 3) transform(data_tuples). 4) unsqueeze(0). 5) predict_with_uncertainty or direct model inference. 6) format_results. 7) Display.

    RESULT CARDS (D-06/D-07/D-08, APP-03): st.columns(3) with st.markdown(unsafe_allow_html=True). Ctr card: 2px solid #ff4b4b border, border-radius 8px, padding 16px, 28px/700 value #262730, 14px/400 subtitle+CI #808495. Secondary cards: 1px solid #e0e0e0, 24px/600 value. CSS per UI-SPEC Result Card CSS Contract. No bootstrap: show "Point estimate (no CI)".

    ctFP HEATMAP (D-09/D-10, APP-05): st.subheader("Model Input Visualization (ctFP)"). plt.subplots(1,2,figsize=(10,4)). Ch0: viridis, Ch1: plasma. aspect=equal, origin=lower. Colorbar fraction=0.046. Labels per UI-SPEC. plt.tight_layout(), st.pyplot(fig), plt.close(fig).

    CITATION (D-12): st.divider(), st.caption with placeholder.

    SPINNER: st.spinner wrapping prediction call.

    NOTES: Lazy matplotlib import. Graceful missing checkpoints. No logic duplication from app_utils.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app.py contains st.set_page_config( with layout="wide"
    - app.py contains @st.cache_resource decorator
    - app.py contains from src.app_utils import validate_input, prepare_ctfp_input, generate_template, format_results
    - app.py contains from src.ctfp_encoder import transform
    - app.py contains from src.model import SimpViT
    - app.py contains st.tabs
    - app.py contains st.data_editor( with num_rows="dynamic"
    - app.py contains st.download_button(
    - app.py contains st.file_uploader(
    - app.py contains st.button with type="primary"
    - app.py contains unsafe_allow_html=True
    - app.py contains #ff4b4b
    - app.py contains viridis and plasma colormaps
    - app.py contains origin and lower for heatmap
    - app.py contains plt.close(fig)
    - app.py contains weights_only=False and weights_only=True
    - app.py contains st.selectbox(
    - python -c "import ast; ast.parse(open('app.py').read())" exits 0
  </acceptance_criteria>
  <done>app.py is syntactically valid, contains all UI components per 05-UI-SPEC.md, imports all 4 utility functions from Plan 01, handles both bootstrap and point-estimate modes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of Streamlit app</name>
  <files>app.py</files>
  <action>
    Human verifies the running Streamlit app matches 05-UI-SPEC.md layout and all interaction states work.
    1. Run streamlit run app.py in project root
    2. Verify page title shows ViT-Ctr: RAFT Chain Transfer Constant Predictor
    3. Verify monomer dropdown with MMA, Styrene, Vinyl Acetate, Custom
    4. Manual Input tab: editable table with 4 columns
    5. File Upload tab: Download Excel Template button + file uploader
    6. Click Download Excel Template: .xlsx downloads with correct columns
    7. Enter 3+ rows sample data, click Predict
    8. If checkpoints exist: 3 result cards (Ctr red border, others gray), ctFP heatmap below
    9. If checkpoints missing: error/warning message
    10. Enter invalid data (conversion=2.0), click Predict: red error banner
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>User confirms app layout, data input, validation errors, prediction display, and ctFP heatmap all work as specified</done>
</task>

</tasks>

<verification>
- python -c "import ast; ast.parse(open('app.py').read())" -- syntax valid
- python -m pytest tests/test_app_utils.py -x -q -- utility tests still pass
- python -m pytest tests/ -x -q -- no regressions
- Manual: streamlit run app.py launches without errors
</verification>

<success_criteria>
- Streamlit app launches and displays all UI components per 05-UI-SPEC.md layout
- Manual data entry works via st.data_editor (APP-01)
- Excel template downloads and file upload works (APP-02)
- Three prediction cards display with Ctr visually prominent (APP-03)
- Input validation catches all invalid patterns with clear error messages (APP-04)
- ctFP dual-channel heatmap renders correctly (APP-05)
- Model loads once via st.cache_resource (APP-06)
</success_criteria>

<output>
After completion, create .planning/phases/05-streamlit-web-application/05-02-SUMMARY.md
</output>
